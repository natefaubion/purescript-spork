{
  "private": true,
  "scripts": {
    "build": "pulp build -- --strict --censor-lib",
    "example:all": "npm run example:todo-mvc",
    "example:todo-mvc": "pulp build -I examples/todo-mvc/src --to examples/todo-mvc/app.js -m Todo.Main"
  },
  "devDependencies": {
    "pulp": "^12.0.0",
    "purescript": "^0.11.6",
    "purescript-psa": "^0.5.0"
  }
}
